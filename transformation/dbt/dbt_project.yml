name: "cinemetrics"
version: "1.0.0"
config-version: 2
profile: "cinemetrics"

model-paths: ["models"]
target-path: "target"
clean-targets: ["target", "dbt_packages"]

models:
  cinemetrics:
    marts:
      +materialized: view
